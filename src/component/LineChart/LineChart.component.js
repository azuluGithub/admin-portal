const LineChart = (props) => {
  const { currentMode } = props;

  return (
    <div
      className="bg-orange-300"
      height="420px"
      style={{ height: '100px' }}
    >
      
    </div>
  );
};

export default LineChart;