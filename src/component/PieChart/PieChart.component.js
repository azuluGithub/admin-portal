const PieChart = ({ id, data, legendVisiblity, height, currentMode }) => {

  return (
    <div
      style={{ height, backgroundColor: currentMode }}
    >
      
    </div>
  );
};

export default PieChart;
